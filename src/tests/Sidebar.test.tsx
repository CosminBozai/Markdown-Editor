import { describe, it } from "vitest";

describe("Sidebar", () => {
  it("should call toggleTheme when clicking the toggle element");
  it("should display the documents");
  it("should open new document modal when clicking the button");
});
